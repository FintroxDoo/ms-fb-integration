# dependencies
node_modules/

# build
dist/
coverage/

# env / secrets
.env
.env.local
.env.staging

# helm secret overrides (injected at deploy, never committed)
values-secret.yaml
**/values-secret.yaml

# local TLS certs (self-signed, dev only)
certs/

# prisma local db
*.db
*.db-journal
prisma/dev.db
prisma/*.db

# logs
*.log
npm-debug.log*

# os / editor
.DS_Store
.idea/
.vscode/
