apiVersion: v1
kind: Service
metadata:
  name: {{ include "ms-fb-integration.tms.fullname" . }}
  labels:
    {{- include "ms-fb-integration.tms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ms-fb-integration.tms.selectorLabels" . | nindent 4 }}
